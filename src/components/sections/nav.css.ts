import { style } from '@vanilla-extract/css';

export const NAV_WRAPPER = style({
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'center',
  alignItems: 'center',
  marginBottom: '2rem',
});
